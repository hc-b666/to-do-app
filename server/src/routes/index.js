const userRoutes = require('./user-routes');
const taskRoutes = require('./task-routes');
const projectRoutes = require('./project-routes');

module.exports = { userRoutes, taskRoutes, projectRoutes };
